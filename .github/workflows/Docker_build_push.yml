name: CI - Build and Push to Docker Hub

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-push:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          file: ./Dockerfile
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/2025_website:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/2025_website:${{ github.run_number }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Log out from Docker Hub
        if: always()
        run: docker logout


#   deploy-to-ec2:
#     runs-on: ubuntu-latest
#     needs: dockerbuildpush
#     steps:
#       - name: Deploy to EC2 via SSH
#         uses: appleboy/ssh-action@v0.1.10
#         with:
#           host: ${{ secrets.EC2_HOST }}
#           username: ubuntu
#           key: ${{ secrets.EC2_SSH_KEY }}
#           script: |
#             echo " Pulling latest Docker image..."
#             docker login -u ${{ vars.DOCKERHUB_USERNAME }} -p ${{ secrets.DOCKERHUB_TOKEN }}
#             docker pull ${{ vars.DOCKERHUB_USERNAME }}/2025_website:latest

#             echo " Stopping old container (if any)..."
#             docker stop 2025_website || true
#             docker rm 2025_website || true

#             echo " Running new container..."
#             docker run -d -p 80:80 --name 2025_website ${{ vars.DOCKERHUB_USERNAME }}/2025_website:latest
